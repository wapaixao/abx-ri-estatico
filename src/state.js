let DATA=null, reportType='BP', viewMode='sideTotal', selected=new Set(), selectedPeriods=new Set(), summaryParticipations=false, summarySort='total', summaryMetric='all';
const fmt=new Intl.NumberFormat('pt-BR',{maximumFractionDigits:0});
const LOGOS={};
