function findRow(label){return rep().rows.find(r=>String(r.descricao).toUpperCase()===label)}function total(label,p){const row=findRow(label);return row?rowTotal(row,p):0}function totalPeriods(label,periods){return periods.reduce((s,p)=>s+total(label,p),0)}function pctFromRL(v,rl){return rl?((v/rl)*100).toFixed(1).replace('.',',')+'%':'–'}function pct(v,p){return pctFromRL(v,total('RECEITA LIQUIDA',p))}
