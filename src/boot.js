function loadData(attempt=1){const url=new URL('data.json',window.location.href);url.searchParams.set('v',Date.now());fetch(url.href,{cache:'no-store'}).then(r=>{if(!r.ok)throw new Error('HTTP '+r.status);return r.json()}).then(d=>{DATA=d;initSelection();renderSelectors();render();initAuth()}).catch(e=>{if(attempt<3){setTimeout(()=>loadData(attempt+1),700*attempt);return}document.getElementById('tables').innerHTML='<section class="panel table-panel">Erro ao carregar data.json após 3 tentativas: '+esc(e.message)+'<br><small>Atualize a página ou limpe o cache do navegador.</small></section>';initAuth()})}loadData();
